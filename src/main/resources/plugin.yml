name: "${project.name}"
version: "${project.version}"
main: "${project.groupId}.${project.artifactId}.${project.name}"
website: "${project.url}"
softdepend: [LogBlock, HawkEye, SWatchdog, CoreProtect, Prism, NoCheat, NoCheatPlus, AntiCheat, MineBansAntiSpam]
loadbefore: [Essentials, CommandBook]